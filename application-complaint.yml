server:
  port: 8081
spring:
  application:
    name: COMPLAINT-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/complaintdb
    username: root
    password: Johns@2000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
